## How To Run

Ensure both the client folder and server folder are inside the same folder. 

Start server:
```
cd server
npm install
nodemon index
```

Start client
```
cd client
npm install
npm start
```